#include "../main.hpp"


int main()
{
    vector<int> a;
    for (size_t i = 0; i < 5025; i++){
        a.push_back(i);
    }
    // print_vec(a);
	print_vec(a);
    // vector<int> b(a);
	// std::cout << "vector b(a)" << std::endl << vecToS(b, "b", b.capacity()) << std::endl;
	// std::cout << std::endl;
}